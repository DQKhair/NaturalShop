@model IEnumerable<PJ_SanPhamTieuDung_.Net.Models.DanhMucSanPham>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenDanhMucSanPham)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenDanhMucSanPham)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaDanhMucSanPham }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaDanhMucSanPham }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaDanhMucSanPham })
        </td>
    </tr>
}

</table>
